import React from 'react';
import { ShoppingBag, Menu } from 'lucide-react';

const Navbar = () => (
  <nav className="fixed w-full z-50 bg-white/80 backdrop-blur-md border-b border-gray-100 px-6 py-4 flex justify-center items-center">
    <h1 className="text-2xl font-serif tracking-widest font-bold">LUXE COORDS</h1>

  
  </nav>
  
);

export default Navbar;